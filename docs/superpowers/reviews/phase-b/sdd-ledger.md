# SDD ledger — plan: docs/superpowers/plans/2026-09-10-android-v2-phase-b-openclaw.md

Branch: android-v2 (main checkout; no worktree — android/local.properties is git-ignored and required for the DAT Maven token). Phase A complete at a067ed4.
Task 1: started (base 173262e, implementer opus)
Task 1: implementer DONE_WITH_CONCERNS (commit 32d6990; 57/57 unit; debug+release+androidTest compile green; deviations: TestBitmaps via Class.forName not sun.misc.Unsafe; null-decode guard in RTMP/currentFrame; resetForTests does not clear lastSessionError; publishFrame owner check races stopCamera → Task 4 must tolerate a stale latestFrame)
Task 1: complete (commits 173262e..32d6990, review clean; 57/57 unit; ⚠️ androidTest access to internal resetForTests relies on AGP friend paths — compile claimed green by implementer)
Task 1: minor (deferred → Task 4 pointer): publishFrame owner check is a TOCTOU vs stopCamera (GlassesSessionManager.kt:339-341) — camera.snap must not treat latestFrame as proof the camera is live; pair with currentCameraOwner/sessionState checks
Task 1: minor (deferred → Task 4 pointer): RTMPStreamingViewModel never calls publishFrame, so latestFrame is null during an RTMP broadcast while the camera is busy — decide whether RTMP should publish or camera.snap returns NO_FRAME/busy
Task 1: minor (deferred): WearablesViewModel.errorEvents SharedFlow unused by UI (seam for Task 8 toast hoist); resetForTests doesn't clear lastSessionError; FakeRegistrationGateway.calls unused (order assertion missing in disconnectStopsStreamThenSessionThenUnregisters); TestBitmaps relies on sun.misc.Unsafe.allocateInstance; takePhoto decode now on the single-slot frameDispatcher
Task 2: started (base 32d6990, implementer sonnet)
Task 2: implementer DONE (commit 0473885; 70/70 unit; debug+release green; Tink API verified via javap; no deviations)
Task 2: review — spec ❌ on one plan-mandated item: normalizeForAuth uses Unicode isLetterOrDigit vs the plan's '[a-z0-9._-]' shorthand (OpenClawDeviceIdentity.kt:95-98)
Task 2: parked — ruling: keep the Unicode-aware filter; it matches iOS's CharacterSet.alphanumerics semantics (research §2.4), which is the only known-good client; actual inputs (platform 'android', empty deviceFamily) are ASCII so no observable difference; plan wording was an imprecise shorthand
Task 2: minor (deferred): add a 'different seed → different deviceId' test; report's javap evidence is paraphrased
Task 2: complete (commits 32d6990..0473885, 1 parked; 70/70 unit; debug+release green)
Task 3: started (base 0473885, implementer opus)
Task 3: implementer DONE (commit aa5a786; 95/95 unit, 25 new; debug+release green; concerns: production backoff/tick constants not asserted; R8 strips the service until referenced; chatEvents replay=0 → Task 6 must subscribe before connect())
Task 3: complete (commits 0473885..aa5a786, review clean; 95/95 unit; §8.6 items 1-6 verified field-by-field)
Task 3: minor (deferred → Task 6 pointers): OpenClawErrorReason.Transport never constructed (transport failures reach UI only as Reconnecting→MaxRetries with no cause) — surface the cause; chatEvents replay=0 + tryEmit result ignored → subscribe before connect() and log drops; sendChatMessage returns true pre-hello (gate on Connected or document); ScriptedGateway.await() consumes non-matching frames (add a peek for Task 6 tests)
Task 3: minor (deferred): dispatchInvoke catches CancellationException implicitly (rethrow before generic catch; timeout/exception branches untested); startConnection() doesn't cancel tickJob (implicit invariant); production RECONNECT_DELAYS_MS/TICK_INTERVAL_MS constants unasserted; unused chatEvents field in the test class (plan-mandated)
Task 4: started (base aa5a786, implementer opus)
Task 4: implementer DONE_WITH_CONCERNS (commit 2a859a5; 109/109 unit; debug+release+androidTest compile green; liveFrame() gate = owner!=null && STARTED; RTMP now publishes preview; foreground gate → NOT_READY; concerns: RTMP publish inspection-only; SessionFrameProvider.create() untestable on JVM; device.status pre-permission constructs the manager singleton; KDoc references a README note Task 8 must add)
Task 4: review — spec ✅, quality Needs fixes: 3 Important (plan-mandated): (1) hasActiveDevice reads activeDevice.value which is null on the first command after process start (GlassesFrameProvider.kt:121-122) → camera.list/device.status lie; (2) capturer fallback gated only on currentCameraOwner==null can win the camera race against a feature between acquire() and addCamera(); (3) the liveFrame() gate (the task's one deviation) has no regression test
Task 4: ruling — fix now: (1) suspend awaitActiveDevice(≤1500 ms) for camera.list/device.status + install() calls sessionManager.startMonitoring() (idempotent); (2) fallback only when sessionManager.ownerCount == 0 (no claims), else wait-then-NO_FRAME, documented in KDoc; (3) add SessionFrameProviderTest staleFrameAfterStopCameraIsNotServed; fold in minors: foreground default ?: false, awaitLiveFrame predicate uses the emitted value
Task 4: minor (deferred): scaled bitmap never recycled; FrameSnapshot data class over ByteArray (identity equals); request.timeoutMs parsed but unused (→ Task 7 Timeout outcome); no age bound on latestFrame (iOS parity); KDoc references a README note Task 8 must add
Task 4: fix round 1/5 started (fresh implementer opus)
Task 4: fix round 1/5 implemented (commit a8d0136; 111/111; builds green; concerns: Task 6 brief's fake must implement awaitActiveDevice; install() constructs the manager in onCreate; status/list may take ≤1.5 s without glasses; README note wording for Task 8) — scoped re-review dispatched
Task 4: fix round 1/5 (4 addressed, 1 half-open — staleFrameAfterStopCameraIsNotServed does not pin the liveFrame() gate because stopCamera() clears owner and frame together; commits 2a859a5..a8d0136)
Task 4: ruling for round 2 — add frameIsNotServedWhileSessionIsPaused (publish under a live owner, drive the fake session to PAUSED, assert hasFrame==false, streamStatus=="waiting", snapshot with the claim present → NO_FRAME, captureCalls==0) which pins the sessionState==STARTED half; the owner half is structurally guaranteed by stopCamera clearing both. Also harden startMonitoring(): wrap flow creation so a synchronous throw cannot crash the process from Application.onCreate
Task 4: fix round 2/5 started (fresh implementer sonnet)
Task 4: fix round 2/5 implemented (commit 5dc880e; 113/113; builds green; deviation: startMonitoring uses CoroutineStart.LAZY + start() so a synchronous throw cannot clobber the deviceJob null-out) — scoped re-review dispatched
Task 4: fix round 2/5 (2 addressed, 0 open — frameIsNotServedWhileSessionIsPaused pins the STARTED half of the gate (mutation-checked); startMonitoring hardened with LAZY start + runCatching; commits a8d0136..5dc880e)
Task 4: complete (commits aa5a786..5dc880e, review clean after 2 fix rounds; 113/113 unit)
Task 4: minor (deferred): resetForTests() does not reset deviceJob (a test calling startMonitoring → resetForTests → startMonitoring short-circuits)
Task 5: started (base 5dc880e, implementer sonnet)
Task 5: implementer DONE (commit 2e458c2; 121/121 unit; builds green; concerns: 3 tests take ~5 s each on MockWebServer.shutdown() waiting for an un-acked close; intl endpoint availability unverified; AudioRecordPcmSource uncovered)
Task 5: review — spec ✅, quality Approved; 2 Important (plan-mandated, test-only): (1) test fixture never acks the client close (no onClosing override) so MockWebServer.shutdown() blocks 5 s in 3 tests; (2) Thread.sleep waits in transportFailureAfterStopIsSilent and fullSessionRunTaskAudioResultsFinishTask
Task 5: ruling — fix both now in fix round 1 (test fixture onClosing ack; latch-based waits; keep production untouched); minors deferred: header() helper dedup, AudioRecordPcmSource scope not cancelled, stop() re-entrancy guard
Task 5: ⚠️ → Task 6 pointer: FunASRService.switchAudioSource only swaps the PcmAudioSource; the ViewModel must call BluetoothAudioManager SCO start/stop in lockstep (pattern OmniRealtimeService.kt:234-245); decide whether the 3 English onError strings ('Microphone unavailable', 'Connection failed', 'ASR task failed') are shown verbatim → localize in Task 6 if so
Task 5: fix round 1/5 started (fresh implementer sonnet)
Task 5: fix round 1/5 implemented (commit 1c84af2; 121/121; FunASRServiceTest 16 s → 1.4 s; one 700 ms bounded wait remains on the post-stop path, documented) — scoped re-review dispatched
Task 5: fix round 1/5 (2 addressed, 0 open — onClosing ack in the fixture; latch waits; commits 2e458c2..1c84af2)
Task 5: complete (commits 5dc880e..1c84af2, review clean after 1 fix round; 121/121 unit)
Task 6: started (base 1c84af2, implementer opus)
Task 6: implementer DONE_WITH_CONCERNS (commit ac4c0ea; 133/133 unit; builds green; 442/442 string keys; concerns: glasses-mic chip resets to phone after each utterance (stopListening stops SCO → receiver flips source); BluetoothAudioManager.switchAudioSource(BLUETOOTH_MIC) doesn't wait for SCO-connected (pre-existing); cleartext permitted globally; no path out of Error(MaxRetries) except settings Connect; deviations: AutoMirrored Send icon, 3 extra ASR-error strings, deliver() Connected gate, SCO start/stop, APIProviderManager bind before static read)
Task 6: review — spec ✅, quality Needs fixes: 1 Important (plan-mandated): glasses-mic selection reverts to phone after every utterance (finishListening stops SCO each time → BluetoothAudioManager receiver flips _currentAudioSource to PHONE_MIC → chip and next startListening read the flipped flow; AudioRecord also opens before the SCO route exists on every turn)
Task 6: ruling — fix now: ViewModel-owned desiredAudioSource (chip + startListening read it; the manager's flow only reports the live route); keep SCO up while the chat screen is active and glasses is desired (start on selection/first listen, stop on leaveScreen/onCleared, not per utterance); before starting ASR on glasses wait bounded (≤3 s) for SCO-connected using an existing manager signal or a minimal scoConnected StateFlow added to BluetoothAudioManager (report it); fold in minors: sendAsrText clears asrError; drop redundant Link import
Task 6: minor (deferred): switchAudioSource silently diverges when SCO unavailable (chip says phone, recognizer on VOICE_COMMUNICATION); all non-Ok snapshot outcomes collapse to openclaw_chat_noframe; ASR transport detail passes raw English through the localized wrapper; suppressed send appends a bubble with no signal
Task 6: fix round 1/5 started (fresh implementer opus)
Task 6: fix round 1/5 implemented (commit 242e62c; 139/139; builds green; 443/443 keys; no plumbing change — manager already exposed isBluetoothScoConnected; test seam OpenClawAudioRoute added) — scoped re-review dispatched
Task 6: fix round 1/5 (2 addressed, 0 open — desiredAudioSource; session-scoped SCO; bounded route wait with localized fallback notice; 6 new tests; commits ac4c0ea..242e62c)
Task 6: complete (commits 1c84af2..242e62c, review clean after 1 fix round; 139/139 unit)
Task 6: minor (deferred): switchAudioSource() mid-utterance forwards to the running recognizer without the bounded SCO wait; SCO held (media muted) while the glasses chip is selected; no SCO re-arm if the link drops mid-session
Task 7: started (base 242e62c, implementer opus)
Task 7: implementer DONE_WITH_CONCERNS (commit 0aaa883; 143/143 unit; builds green; extra: RTMP URL logging now server-only (key leak fix); request.timeoutMs still not wired into the capturer (brief only adds Timeout branch); 15 s aggregate budget; onDisconnectRtmp now async on an executor)
Task 7: review — spec ✅, quality Approved; 1 Important (pre-existing, adjacent to the closed ledger row): RTMPStreamingService.startStreaming sets isStreaming=true after rtmpClient.connect() (:199-205); RtmpClient.connect() calls onConnectionFailedRtmp synchronously on a malformed URL → stopStreaming() nulls the encoder → loop starts with encoder==null and spins at 100% CPU with no exception; VM stuck in Connecting until Stop
Task 7: ruling — fix now: set isStreaming=true before connect() and bail after connect() if encoder==null || rtmpClient==null (state Error); fold in minors: fence stale ConnectCheckerRtmp callbacks with a client identity check (self !== rtmpClient → return); re-check isStreaming before publishing the encoder-failure Error; wrap getRtmpBitrate/saveRtmpBitrate/saveRtmpStreamKey/deleteRtmpStreamKey in the same try/catch as getRtmpStreamKey
Task 7: minor (deferred): StreamingState.Disconnected is unreachable in rtmp 2.2.6 (onDisconnectRtmp only fires from the public disconnect()) — keep the guard, note dead rtmp_disconnected strings; server URL field not validated key-free (run RtmpUrlSplitter in updateRtmpUrl) → Phase D polish; migration/flag-guard/stopLock have no JVM coverage → Task 9 device check
Task 7: fix round 1/5 started (fresh implementer sonnet)
Task 7: fix round 1/5 implemented (commit 77bb737; 147/147; builds green; generation fencing + Stop/error race verified by inspection; malformed-URL repro traced by hand → Task 9 device check) — scoped re-review dispatched
Task 7: fix round 1/5 (4 addressed, 1 half-open — connectionGeneration bumped only in startStreaming(), not in stopStreaming()'s swap, so a client retired by Stop without a restart can still flip Idle → Streaming/Error via a late onConnectionSuccessRtmp/onConnectionFailedRtmp/onAuthErrorRtmp; commits 0aaa883..77bb737)
Task 7: fix round 2/5 started (fresh implementer sonnet): bump connectionGeneration when stopStreaming() retires the client
Task 7: fix round 2/5 implemented (commit c553809; 147/147; builds green; inspection-verified) — scoped re-review dispatched
Task 7: fix round 2/5 (1 addressed, 0 open — generation advances on stop; commits 77bb737..c553809)
Task 7: complete (commits 242e62c..c553809, review clean after 2 fix rounds; 147/147 unit; device checks for Task 9: malformed-URL start, late callback after Stop, rerunRtmpMigrationForTests)
Task 8: started (base c553809, implementer sonnet)
Task 8: implementer DONE (commit eafb608; 147/147; builds green; edge-to-edge verified by inspection; upstream report drafted only)
Task 8: complete (commits c553809..eafb608, review clean; 147/147)
Task 8: minor (deferred): root README 'Display 眼镜相机支持' phrasing less precise than CHANGELOG ('work as camera devices'); R.string.version now unreferenced
Task 9: started (base eafb608, verifier opus)
Task 9: verifier DONE_WITH_CONCERNS (commit f7b4953; unit 147 both variants; instrumented 20/20 ×3 runs; checklist 20 PASS / 1 FAIL / 3 N/A of 24; builds green)
Task 9: defect D1 (FAIL item 9, Important): OpenClaw chat Snap & Send always NO_FRAME — OpenClawViewModel holds a manager claim (enterScreen acquire) without publishing frames, so SessionFrameProvider's ownerCount==0 gate (Task 4 fix) never falls back to the capturer; same for gateway camera.snap while the chat is foregrounded
Task 9: defect D2 (Important): RTMP can hang in Connecting on restart after a failed attempt (videoWidth never reset → 10 s first-frame timeout is a no-op); Stop from that state produced an ANR
Task 9: defects D3-D6 (minor): App Language persisted but not applied after restart (pre-existing, API 31 no-op); OpenClaw settings edits saved only by Connect; settings 'Commands' label wraps one char per line; MockDeviceKit toggles misreport after re-entry (Phase A deferred, no SDK getters)
Task 9: test-code deviations: explicit ': Unit' on two onMain tests; DAT 0.9.0 moves the SESSION to PAUSED on captouch tap (assertion corrected); fold() stops the session
Task 9: review — spec ✅, quality Approved; 1 Important (brief-prescribed): OpenClawNodeServiceInstrumentedTest handshake test uses a fixed Thread.sleep(200) before sending the chat event (chatEvents replay 0 subscription race) — replace with a subscription signal in the fix wave; minor: AudioRecordPcmSourceInstrumentedTest 300 ms negative wait needs a comment
Task 9: item-9 minimal fix (reviewer-verified): GlassesSessionManager.acquire(owner, streams: Boolean = true) + streamingOwnerCount; OpenClawViewModel.enterScreen() acquires with streams=false; SessionFrameProvider.snapshot() gates the capturer fallback on streamingOwnerCount == 0 (owners.isEmpty() still drives stopSession)
Task 9: complete (commits eafb608..f7b4953, review clean; fix items carried to the final fix wave)
Final review (fable): Ready to merge WITH FIXES. Critical: C1 chat session-only claim blocks the capturer (ownerCount gate) → camera-intent claims in the manager + hasCameraClaim gate; C2 RTMP Error keeps the camera borrowed and videoWidth never resets → Connecting hang on restart + ANR on Stop. Important: I1 shared capturer owner across two providers (single instance + captureMutex); I2 device monitoring unrestartable after flow completion + started before BLUETOOTH_CONNECT; I3 connect rejected with non-NOT_PAIRED code → stuck Connecting (Transport error, stop reconnect); I4 stopBluetoothSco early-return leaves MODE_IN_COMMUNICATION; I5 no OkHttp pingInterval; I6 test gaps. Report: .superpowers/sdd/2026-09-10-android-v2-phase-b-openclaw/final-review-report.md
Final fix wave started (single implementer opus): steps 1-9 of the prescription + Task 9 review's Thread.sleep(200) replacement in the instrumented handshake test
Final fix wave: implemented (commit b09aade; 154/154 both variants; 21/21 instrumented; device checks a/b/c PASS; concerns: instrumented OpenClaw test may leak the app's real port/token on the emulator (test hygiene → Phase C); releaseScoAfterAFailedArmStillCallsStopSco is characterization-only; MockDeviceKit debug toggles worse than documented (first 'Unfolded' tap sends fold) → Phase C; I6(b)/(d) test seams not done) — scoped re-review dispatched
Final fix wave: scoped re-review (opus) — All 9 findings ADDRESSED, no new Critical/Important breakage (commits f7b4953..b09aade). Checked: cameraClaimCount assigned from set size (no underflow on double release), captureMutex suspending (no Main deadlock), provider singleton double-checked + injectable, startMonitoring identity restart, RTMP Error branch keeps UIState.Error without double service stop, rejected-connect under lock + handleDisconnect socket-identity guard, pingInterval LAN client only, SCO change byte-identical for Live AI, locales 450/450, subscriptionCount accessor @VisibleForTesting with 30 s bound
Final fix wave: out-of-scope observations (deferred) — onAuthErrorRtmp never stops the service (isStreaming stays true → Start refused until leaving the screen) → Phase C/D ticket next to I6(b); save-on-back coerces empty port to DEFAULT_PORT (Back no longer discards); deviceJob written from invokeOnCompletion (safe on Main.immediate, needs @Volatile if scope changes); scoRequested per-instance vs process-global audio mode; handleMessage has no socket-identity guard (unreachable today); companion captureMutex serialises captures process-wide; instrumented OpenClaw test writes the app's real EncryptedSharedPreferences (test hygiene → Phase C)
Phase B: COMPLETE (commits a067ed4..b09aade; 154/154 unit both variants; 21/21 instrumented; device checks PASS; records archived to docs/superpowers/reviews/phase-b/)
